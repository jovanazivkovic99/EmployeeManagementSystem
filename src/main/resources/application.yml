spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ems
    username: root
    password: password123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update